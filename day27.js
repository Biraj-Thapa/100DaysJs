//Repeat a String
//write a function called repeatString  that takes two parameters 
//A string tthat needs to be reppeated.
//An integer representing number of time the string should be repeated.
//The function should retrun the input string str the specified number of times and retrun the resulting string.

const repeatedString=(str,num)=>{
    return str.repeat(num)


}
console.log(repeatedString("biraj",5))